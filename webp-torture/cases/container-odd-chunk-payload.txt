# note: An optional chunk with an odd-sized payload, and the pad byte that
#   must follow it.
# expect: ok
# roundtrip: no
# exercises: disk_chunk_size rounds a payload up to an even length, which
#   only matters when something follows the chunk it rounded. Here a JUNK
#   payload of three bytes sits in front of the image, so getting the
#   rounding wrong moves the frame rather than the end of the file.
chunks VP8X JUNK VP8
payload JUNK 010203
width 32
height 32
yac_qi 30
macroblock
coeffs y2 -20
coeffs y[6] 1:4 -2
macroblock
intra_y_mode V_PRED
coeffs y2 12
macroblock count 2
