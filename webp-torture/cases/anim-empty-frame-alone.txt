# note: An animation whose only frame carries no image.
# expect: reject
# anim: reject
# info: reject
# exercises: The same silent drop as anim-empty-frame, with nothing left
#   behind it. The frame list stays empty, and IsValidExtendedFormat()
#   refuses a finished file with no frames at all -- so what the demuxer
#   tolerates in the middle of a sequence it will not tolerate as the whole
#   of one.
frame
frame_duration 100
chunks
lossless
width 16
height 16
argb 0xff800000 x256
