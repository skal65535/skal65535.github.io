# note: A frame whose header fits the canvas but whose image does not.
# expect: reject
# anim: reject
# info: reject
# roundtrip: no
# exercises: The other half of anim-frame-size-mismatch. The ANMF says 16x16
#   at (16,16), which fits; the image inside is 32x32, and since that is the
#   size CheckFrameBounds() ends up testing, the frame runs 16 pixels past
#   the canvas and the file is refused. The header numbers being reasonable
#   is no protection.
canvas_width_minus_one 31
canvas_height_minus_one 31
frame
frame_x 8
frame_y 8
frame_width_minus_one 15
frame_height_minus_one 15
frame_duration 100
lossless
width 32
height 32
argb 0xff102030 x1024
