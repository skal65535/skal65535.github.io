# note: A RIFF size that stops in the middle of the last frame.
# expect: reject
# anim: reject
# info: reject
# exercises: The RIFF length is the ceiling every other length is measured
#   against: the demuxer clamps its buffer to it, so the second frame's own
#   header -- which is intact on disk, and correct -- now declares more
#   bytes than are left, and SizeIsInvalid() refuses it. The bytes are
#   there; the file just says they are not.
riff_size 102
frame
frame_duration 100
lossless
width 16
height 16
argb 0xff800000 x256
frame
frame_duration 100
lossless
width 16
height 16
argb 0xff008000 x256
