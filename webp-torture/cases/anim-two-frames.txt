# note: Two full-canvas lossless frames, the plain animation everything else
#   here is a variation on.
# expect: reject
# anim: ok
# info: ok
# roundtrip: no
# exercises: The whole ordinary path: VP8X with the animation flag, ANIM,
#   then one ANMF per frame carrying a VP8L image the size of the canvas.
#   Both frames are key frames -- frame 1 always is, and frame 2 is because
#   it is opaque and fills the canvas -- so IsKeyFrame() takes its first two
#   branches and neither frame is blended against anything.
frame
frame_duration 100
lossless
width 32
height 32
argb 0xffff0000 x1024
frame
frame_duration 150
lossless
width 32
height 32
argb 0xff00ff00 x1024
